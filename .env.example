# 2 = faster (~30s on CPU), 4 = higher res (~60s on CPU)
ENHANCE_SCALE=2

# Enable face restoration for passport/NIC/licence photos
ENABLE_FACE=true

# Hard safety timeout in seconds (not normally reached due to pre-scaling)
ENHANCE_TIMEOUT=120